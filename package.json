{
  "name": "ti-demand-control",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "deploy": "npm run build && cd server && npm run build && npm start"
  }
}
